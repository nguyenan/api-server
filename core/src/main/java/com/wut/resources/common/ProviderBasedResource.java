package com.wut.resources.common;

import com.wut.provider.Provider;

public class ProviderBasedResource {
	public ProviderBasedResource(Provider provider) {
		// read all the methods of the provider
		
		// and create a corresponding action
	}
}
