package com.wut.resources.common;

public class SimpleResource {
	public SimpleResource() {
		// reads all methods using reflection
		
		// if method has annotation "@Action"
		
		// read parameters for annotations @Required & @Optional & @Type (can get away without this)
	}
}
