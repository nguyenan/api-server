package com.wut.model.message;

import com.wut.model.map.MessageData;

public class WarningMessage extends MessageData { // 

	public WarningMessage(int id, String name, String desc) {
		super(id, name, desc);
	}

}
