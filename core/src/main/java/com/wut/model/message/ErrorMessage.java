package com.wut.model.message;

import com.wut.model.Data;
import com.wut.model.map.MessageData;

public class ErrorMessage extends MessageData { // 

	public ErrorMessage(int id, String name, String desc) {
		super(id, name, desc);
	}

}
