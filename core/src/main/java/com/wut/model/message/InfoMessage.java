package com.wut.model.message;

import com.wut.model.map.MessageData;

public class InfoMessage extends MessageData { // 

	public InfoMessage(int id, String name, String desc) {
		super(id, name, desc);
	}

}
